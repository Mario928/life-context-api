# Core dependencies for GPS and Audio APIs
fastapi==0.104.1
uvicorn[standard]==0.24.0
psycopg2-binary==2.9.9
python-dotenv
python-multipart==0.0.6

# Audio API specific
azure-storage-blob==12.19.0
pydub==0.25.1

# NOTE: Whisper API dependencies (torch, faster-whisper) removed
# They require FFmpeg system libraries not available on standard App Service
# Whisper API will be deployed separately on GPU VM or Docker container
